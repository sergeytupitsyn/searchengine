server:
  port: 8080

spring:
  datasource:
    username: root
    password: Gslsuhtpl-43
    url: jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

indexing-settings:
  sites:
    - url: http://avia-simply.ru
      name: Авиация, понятная всем
    - url: https://www.lenta.ru
      name: Лента.ру